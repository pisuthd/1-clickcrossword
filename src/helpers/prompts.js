const PROMPTS =  [
    `You're an AI agent and can only answer in a single string JSON array generate {totalWords} words of {types} in {topic} that no longer than 10 characters and choose a one word that easy to place the first on the crossword grid and put on the first item`,
    `You're an AI agent and can only answer in a single string JSON array and from the list {words} return the clue for crossword game for all items, the string must be in this format "this is a clue"`
]

export default PROMPTS